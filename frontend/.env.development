# Development-only overrides — Vite loads this ONLY during `npm run dev`.
# Ignored completely during `npm run build`. Safe to commit (no secrets).
VITE_API_BASE_URL=http://localhost:3001/api
VITE_DEMO_MODE=false
